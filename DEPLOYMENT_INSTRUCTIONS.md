# خطوات تشغيل موقع "مسار" على Google Apps Script

عشان تشغل الموقع بتاعك ويكون له رابط خاص بيك، اتبع الخطوات دي بالظبط:

1. **افتح Google Apps Script:**
   - روح للرابط ده: [script.google.com](https://script.google.com/home)
   - اضغط على زر **"مشروع جديد" (New Project)**.

2. **تعديل ملف الكود (Code.gs):**
   - هتلاقي ملف مفتوح قدامك اسمه `Code.gs`.
   - امسح كل اللي فيه وحط الكود اللي موجود في ملف `Code.gs` اللي أنا بعتهولك.
   - اضغط على علامة الحفظ (الديسكت).

3. **إضافة ملف الواجهة (Index.html):**
   - اضغط على علامة الزائد **(+)** اللي جنب كلمة "Files" (ملفات).
   - اختار **"HTML"**.
   - سمّي الملف ده `Index` (خلي بالك الحرف الأول كابيتال ومن غير `.html`).
   - امسح كل الكود اللي ظهرلك وحط الكود اللي موجود في ملف `Index.html` اللي أنا بعتهولك.
   - اضغط على علامة الحفظ.

4. **نشر الموقع (Deployment):**
   - اضغط على الزر الأزرق الكبير **"Deploy"** (نشر) في أعلى الصفحة.
   - اختار **"New deployment"** (نشر جديد).
   - اضغط على علامة الترس (Select type) واختار **"Web app"**.
   - في خانة "Description" اكتب أي حاجة (مثلاً: Masar V1).
   - في خانة "Execute as" خليها **"Me"** (أنا).
   - في خانة "Who has access" خليها **"Anyone"** (أي شخص).
   - اضغط على زر **"Deploy"**.

5. **منح الصلاحيات:**
   - هيظهرلك نافذة بتطلب صلاحيات، اضغط على **"Authorize access"**.
   - اختار حسابك على Google.
   - لو ظهرلك تحذير "Google hasn't verified this app"، اضغط على كلمة **"Advanced"** تحت، وبعدين اضغط على **"Go to Masar (unsafe)"**.
   - اضغط **"Allow"**.

6. **مبروك!**
   - هيظهرلك رابط تحت كلمة **"Web app URL"**.
   - ده الرابط بتاع موقعك، انسخه وافتحه من موبايلك أو كمبيوترك.

### ملاحظات هامة:
- الموقع بيحفظ بياناتك على المتصفح بتاعك (localStorage)، يعني لو فتحته من متصفح تاني البيانات مش هتكون موجودة، عشان كدة ضفتلك ميزة "تصدير البيانات" في صفحة الملف الشخصي عشان تقدر تنقلها براحتك.
- مواقيت الصلاة بتعتمد على المحافظة اللي بتختارها في الأول.
