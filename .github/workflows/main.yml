name: Manual Build APK
on:
  workflow_dispatch: # هذا السطر السحري اللي هيظهر لك الزرار

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Hello World
        run: echo "الآن الماكينة تعمل بنجاح"
